{
  "mcpServers": {},
  "systemConfig": {
    "install": {
      "baseUrl": "${BASE_URL}"
    },
    "oauthServer": {
      "enabled": true,
      "allowedScopes": ["read", "write"],
      "dynamicRegistration": {
        "enabled": true
      },
      "requireClientSecret": false
    }
  }
}
